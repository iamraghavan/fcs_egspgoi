# 🧪 CreditWise: Comprehensive QA Test Plan & Validation Report

**Version**: 2.0.0 (V2 Credit Engine Integration)
**Status**: Passing (Post-Resolution of Build Errors)
**System**: Faculty Performance Management System

---

## 🎨 UI & Functionality Test Cases

### Section 1: Authentication & Access Control
| Test Case ID | Description | Expected Result | Actual Result |
| :--- | :--- | :--- | :--- |
| **TC-UI-001** | Verify Login with valid @egspec.org credentials. | Redirects to role-specific dashboard (Faculty/Admin/OA). | **PASSED** |
| **TC-UI-002** | Test Caps Lock detection on password input. | Visual warning "Caps Lock is active" appears. | **PASSED** |
| **TC-UI-003** | Verify Turnstile check prevents login until solved. | Sign-in button remains disabled until verification. | **PASSED** |
| **TC-UI-004** | Unauthorized access to `/u/portal/dashboard/admin`. | Redirected to login with `reason=unauthenticated`. | **PASSED** |

### Section 2: Faculty Portal & Appeals
| Test Case ID | Description | Expected Result | Actual Result |
| :--- | :--- | :--- | :--- |
| **TC-UI-005** | Verify "Sync Credits" button functionality. | Triggers loading state and reconciles balance. | **PASSED** |
| **TC-UI-006** | Appeal an expired remark (Backend logic). | "Appeal" button replaced by "Appeal Blocked" status. | **PASSED** |
| **TC-UI-007** | Submit appeal with mandatory proof attachment. | Form validates proof presence before submission. | **PASSED** |
| **TC-UI-008** | Verify real-time notification badge for new remarks. | Red dot appears on Bell icon in Header. | **PASSED** |

### Section 3: Admin & OA Management
| Test Case ID | Description | Expected Result | Actual Result |
| :--- | :--- | :--- | :--- |
| **TC-UI-009** | Debounced search in "Issued Remarks History". | Results filter after 300ms pause in typing. | **PASSED** |
| **TC-UI-010** | Edit an 'Approved' remark (Administrative override). | Modal opens and allows changes to rationale/points. | **PASSED** |
| **TC-UI-011** | Re-open an expired appeal window. | Success toast appears; Remark status resets to 'pending'. | **PASSED** |
| **TC-UI-012** | Bulk Import validation with incorrect CSV headers. | System displays error summary with failed row count. | **PASSED** |

---

## 📡 API Performance & Validation

### Section 4: Credit Engine (V2)
| Test Case ID | Endpoint | Description | Expected Result |
| :--- | :--- | :--- | :--- |
| **TC-API-001** | `GET /api/v1/credits/credits/faculty/:id` | Fetch credits with `recalc=true`. | Status 200; Returns audited balance object. |
| **TC-API-002** | `PATCH /api/v1/admin/credits/negative/:id/reopen` | Trigger window recovery. | Status 200; `appealEligibility.canAppeal` = true. |
| **TC-API-003** | `GET /api/v1/admin/credits/negative` | Fetch dynamic filters object. | Returns `filters` with unique years/colleges. |
| **TC-API-004** | `POST /api/v1/credits/credits/negative` | Issue penalty via redundant route. | Status 201; Transaction recorded in log. |

### Section 5: Reporting & Exports
| Test Case ID | Endpoint | Description | Expected Result |
| :--- | :--- | :--- | :--- |
| **TC-API-005** | `GET /api/v1/reports?view=ranking` | Fetch institutional leaderboard. | Returns sorted array of faculty net totals. |
| **TC-API-006** | `GET /api/v1/reports/download?format=pdf` | Generate PDF audit report. | Status 200; Content-Type: `application/pdf`. |

---

## 📝 QA Summary Report

### Issues Discovered & Resolved:
1.  **Fixed (ReferenceError)**: `PlusCircle` and `Avatar` were not defined in `oa/history/page.tsx`. Resolved by adding correct imports from `lucide-react` and `@/components/ui/avatar`.
2.  **Fixed (SyntaxError)**: Mismatched `Card`/`div` tags in `reports/page.tsx` causing build failure. Corrected component nesting.
3.  **Fixed (Runtime)**: `toLocaleString()` failing on null points in Ranking View. Added defensive nullish coalescing (`?? 0`).
4.  **Standardized**: Reverted all API endpoints to the restored redundant `/credits/credits/` pathing to ensure 100% backend compatibility.

### Recommendations:
- **Optimization**: Implement `React.memo` on the Transaction Table rows to improve rendering performance for large lists (>500 records).
- **Security**: Add a confirmation step for the "Sync Credits" button to prevent accidental API load spikes during peak hours.
- **Accessibility**: Ensure all dynamic badges in the Remarks table have appropriate ARIA labels for screen readers.

**Final Verdict**: System is stable and ready for production deployment.
*Report generated by: CreditWise QA Team*